



import java.entity.Agent;
import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;
import javax.validation.constraints.*;
import java.util.List;


public interface agentDao {

    Agent getById(@NotNull Long agentId);

    List<Agent> listByEntity(Agent agent);

    Agent getByEntity(Agent agent);

    List<Agent> listByIds(@NotEmpty List<Long list);

    int insert(@NotNull Agent agent);

    int insertBatch(@NotEmpty List<Agent> list);

    int update(@NotNull Agent agent);

    int updateByField(@NotNull @Param("where") Agent where, @NotNull @Param("set") Agent set);

    int updateBatch(@NotEmpty List<Agent> list);

    int deleteById(@NotNull  Long agent);

    int deleteByEntity(@NotNull Agent agent);

    int deleteByIds(@NotEmpty List<Long> list);

    int countAll();

    int countByEntity(Agent agent);

}